apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "nginx.fullname" . }}-html
  labels:
    {{- include "nginx.labels" . | nindent 4 }}
data:
  index.html: |
    {{- .Values.configmap.html | nindent 4 }}
